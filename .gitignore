node_modules/
.DS_Store
*.log

# API keys and secrets — never commit (only .env.example is tracked)
.env
.env.local
.env.*.local
.venv/
.venv-agents/
.venv-wsl/

# Large research PDFs (keep local; export characteristics.csv instead)
brand/inputs/**/*.pdf

# Regenerable pipeline image outputs (rebuild via npm scripts; recipes/JSON are committed).
# competitor packshots  -> npm run brand:competitor-images  (uses image-overrides.json)
# generated brand boards -> npm run brand:images            (from strategy + a design theme)
brand/inputs/competition/images/
brand/directions/*/identity/images/
