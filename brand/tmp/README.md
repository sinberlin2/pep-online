# Staging folder

Drop new brand images here before they are sorted into `identity/`, `product/`, or `marketing/`.

Use clear filenames if you can (e.g. `can.png`, `logo.png`) — it speeds up sorting.
