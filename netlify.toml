# Netlify — static site, no build step. Only site/ is deployed (brand/ is the generator).
[build]
  publish = "site"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
